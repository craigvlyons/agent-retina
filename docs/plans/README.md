## Active Plans

This directory has been pruned to keep only the current forward path.

The active plans are:

1. `code_source_harness_adoption_plan.md`
   The source-aligned runtime direction for supervised tasks, scoped agents, and the shared worker architecture.

2. `multi_device_specialist_runtime_plan.md`
   The next build program for multi-device specialists, swarm-style delegation, remote transport, parent/child control, and specialist capability adapters over complex external systems like desktop/browser automation.

3. `source_style_followup_continuity_plan.md`
   The source-aligned session continuity and artifact reuse plan for normal follow-up turns, resume, and specialist sidechains.

4. `source_pipeline_fine_detail_alignment_plan.md`
   The deeper pipeline-by-pipeline source audit and cutover plan for the smaller implementation details that make source dependable in practice. This is the clean one-shot replacement track for message-windowing, replacement-state handling, reconstruction, sidechains, compaction precision, and related runtime seams.

5. `source_file_project_inspection_alignment_plan.md`
   The source-aligned implementation plan for stronger file, folder, project, and code inspection behavior in Retina, including read semantics, glob/grep search, inspection continuity, operator visibility, and specialist reuse.

Everything else that previously lived here was removed on purpose after the transcript-first refactor and cleanup work landed. If a small unfinished seam still exists, we will rediscover it through implementation and tests instead of navigating old plan stacks.
